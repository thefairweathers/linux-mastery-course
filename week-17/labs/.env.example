# =============================================================================
# Environment Variables — Three-Tier Application
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# The .gitignore at the repo root excludes .env files.
# =============================================================================

# PostgreSQL
POSTGRES_DB=taskdb
POSTGRES_USER=taskapp
POSTGRES_PASSWORD=changeme_in_production

# Flask API (maps to the DB_* variables the app expects)
DB_NAME=taskdb
DB_USER=taskapp
DB_PASS=changeme_in_production
